rendering:
Form.__str__
Form.as_table
Form._html_output
BoundField.__str__
BoundField.as_widget => gets value, and passes it on to the widget.render
                        gets value form self.field.bound_data(self.data, self.inital) if bound
                        or from self.field.prepare_value(data) if not bound

Widget.render
Widget.get_context